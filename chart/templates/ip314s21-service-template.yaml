apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
  labels:
    app: {{ .Chart.Name }}
    owner: ip314s21
spec:
  selector:
    app: {{ .Chart.Name }}
    owner: ip314s21
  ports:
    - port: 80
      targetPort: {{ .Values.ports.containerPort }}
      protocol: TCP
  type: ClusterIP
